# Rider Feature

Handles delivery management, active orders, route assignments, and rider earnings.

## Layering Rule
- `presentation/`: UI screens and controllers.
- `domain/`: Rider models and repository interfaces.
- `data/`: Rider repository implementations communicating with `ApiClient`.
